<configuration>
  <system.webServer>
    <!-- iisnode: flushResponse required for Server-Sent Events (SSE) streaming -->
    <iisnode flushResponse="true" />
    <handlers>
      <add name="iisnode" path="server.js" verb="*" modules="iisnode"/>
    </handlers>
    <rewrite>
      <rules>
        <rule name="StaticContent" stopProcessing="true">
          <match url=".*" />
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" />
          </conditions>
          <action type="None" />
        </rule>
        <rule name="DynamicContent">
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
          </conditions>
          <action type="Rewrite" url="server.js"/>
        </rule>
      </rules>
    </rewrite>

    <!-- Warm-up: ping /health so Node boots without hitting /server.js directly -->
    <applicationInitialization doAppInitAfterRestart="true">
      <add initializationPage="/health" />
    </applicationInitialization>

    <!-- Disable dynamic compression for Server-Sent Events to avoid buffering/broken streams -->
    <httpCompression>
      <dynamicTypes>
        <add mimeType="text/event-stream" enabled="false" />
      </dynamicTypes>
    </httpCompression>

    <!-- Default document should be the SPA index, not server.js (which is handled via rewrite) -->
    <defaultDocument>
      <files>
        <clear/>
        <add value="index.html" />
        <add value="index.htm" />
      </files>
    </defaultDocument>
  </system.webServer>
</configuration>
