//
// Root entry point
// ----------------
// Loads the Express application from server/index.js — the single
// source of truth for all routes and middleware (dev + production).

require('./server/index');