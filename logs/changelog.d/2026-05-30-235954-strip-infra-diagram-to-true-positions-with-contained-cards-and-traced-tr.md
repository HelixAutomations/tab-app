2026-05-30 / Strip infra diagram to true positions with contained cards and traced traffic / Dropped every concept-stamped element (VNet, subnets, private endpoints, helix-storage) and now show only resources with a real repo citation. Cards are wider with text clipped to fit so names/meta/source never overflow, evidence is a small green/yellow dot in the corner instead of a noisy pill, and the dashed band frames were replaced with single section headers and rules. Traffic is illustrated with per-source lanes so each app's fan-out parks on its own channel; idle edges fade and the hovered card lights its chain in CTA. (~ src/tabs/roadmap/system/SystemInfrastructureView.tsx)
