2026-05-30 / Amalgamate Reception stage snapshot into call detail bar / Stage snapshot pills (Call, Notes, Enquiry, Instruction, Matter) now sit on the right of the existing Call detail bar instead of a second strip beneath it. Added a highlight 'Matter rate' pill at the end. Dropped the separate resolved/onboarding stats block (data is already in the stage pills). (~ src/tabs/Reporting/ReceptionReport.tsx, ~ src/tabs/Reporting/ReceptionReport.css)
